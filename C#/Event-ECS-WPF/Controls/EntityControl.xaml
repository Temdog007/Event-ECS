<UserControl x:Class="Event_ECS_WPF.Controls.EntityControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:Event_ECS_WPF.Controls"
             xmlns:converter="clr-namespace:Event_ECS_WPF.Converters"
             xmlns:ex="clr-namespace:Event_ECS_WPF.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="m_entityControl"
             PreviewMouseRightButtonDown="Entity_PreviewMouseRightButtonDown">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <ex:BindingProxy x:Key="EntityControl" Data="{Binding ElementName=m_entityControl}"/>

        <converter:NotConverter x:Key="notCol"/>
        <converter:BoolToVisibilityConverter x:Key="boolVis"/>
    </UserControl.Resources>

    <Expander>
        
        <Expander.Header>
            <StackPanel Orientation="Horizontal">
                <TextBox Text="{Binding ElementName=m_entityControl, Path=Entity.Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Margin="5,0,0,0">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}ID #{0}">
                            <Binding ElementName="m_entityControl" Path="Entity.ID" UpdateSourceTrigger="PropertyChanged"/>
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </StackPanel>
        </Expander.Header>

        <Expander.ContextMenu>
            <ContextMenu>

                <ContextMenu.ItemsSource>
                    <CompositeCollection>
                        <MenuItem Header="Add Component" ItemsSource="{Binding Source={StaticResource EntityControl}, Path=Data.Project.Components, UpdateSourceTrigger=PropertyChanged}">
                            <MenuItem.ItemTemplate>
                                <DataTemplate>
                                    <MenuItem Header="{Binding}" CommandParameter="{Binding}" Command="{Binding Source={StaticResource EntityControl}, Path=Data.AddComponentCommand}"/>
                                </DataTemplate>
                            </MenuItem.ItemTemplate>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="Show Events" IsCheckable="True" IsChecked="{Binding Source={StaticResource EntityControl}, Path=Data.ShowEvents, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <Separator/>
                        <MenuItem Header="Is Enabled" IsCheckable="True" IsChecked="{Binding Source={StaticResource EntityControl}, Path=Data.Entity.IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    </CompositeCollection>
                </ContextMenu.ItemsSource>

            </ContextMenu>
        </Expander.ContextMenu>
        
        <StackPanel>

            <Expander Header="Events" Background="LightYellow" Visibility="{Binding ElementName=m_entityControl, Path=ShowEvents, Converter={StaticResource boolVis}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ListBox ItemsSource="{Binding ElementName=m_entityControl, Path=Entity.Events, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            </Expander>

            <GroupBox Header="Components">
                <ListBox ItemsSource="{Binding ElementName=m_entityControl, Path=Entity.Components, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <controls:ComponentControl Component="{Binding}" Background="AliceBlue" IsExpanded="{Binding Mode=TwoWay, Path=IsSelected, 
                                                            RelativeSource={RelativeSource AncestorType=ListBoxItem, Mode=FindAncestor}}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>
        </StackPanel>
    </Expander>
</UserControl>
